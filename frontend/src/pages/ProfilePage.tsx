const ProfilePage = () => (
  <div>
    <h1 className="text-2xl font-semibold text-gray-800">Profile</h1>
    <p className="mt-2 text-gray-500">Profile Page — placeholder</p>
  </div>
);

export default ProfilePage;
